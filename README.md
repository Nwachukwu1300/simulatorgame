
  # Build UI with Markdown

  This is a code bundle for Build UI with Markdown. The original project is available at https://www.figma.com/design/eZyJoKqsdgWaGUgeasNnpk/Build-UI-with-Markdown.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  